# Harness 学习整理

> 整理自三篇学习笔记：
> - blog4《拿逆向五步法拆自己项目的源码》(2025-07-19)
> - blog5《把 8 个 Agent 框架切开摆在一起看》(2025-07-19)
> - blog_harness《Harness 架构深度解析·Hermes 对照》(2026-07-27)
>
> 目的：把三篇文章里分散的方法论、案例、对照结论汇成一份可长期查阅的工程笔记，不做理论复述，只留下"下次做 Agent 系统时能直接用"的部分。

---

## 一、Harness 架构速览

### 一句话核心

**Harness 是模型和现实世界之间的唯一接口** —— 模型本身只会"说话"（输出 token），是 Harness 提供工具、上下文、记忆、钩子、权限五套机制，把"会说话的大脑"改造成"能干活的手脚"。2026 年的判断是：**Harness 比 Model 更重要**——换模型（GPT→DeepSeek→Claude）不改变产出质量的下限，改变下限的是 Harness 里存了多少规则、Skills 覆盖了多少场景。

### 组件图

```
        ┌─────────────────────────┐
        │         HARNESS          │
        │  ┌──────┐  ┌──────────┐ │
        │  │Tools │  │ Context  │ │
        │  └──────┘  └──────────┘ │
        │      ↘    MODEL    ↙     │
        │  ┌──────┐  ┌──────────┐ │
        │  │Memory│  │  Hooks   │ │
        │  └──────┘  └──────────┘ │
        │       Permissions        │
        └─────────────────────────┘
```

五个组件围着模型转，模型不直接接触外部世界，所有交互都通过 Harness 中转。

### 五个组件，各自解决什么问题

| 组件 | 解决的问题 | Claude Code 的答案 |
|---|---|---|
| **Tools** | 模型怎么对外部世界产生影响 | 少而精：Read/Write/Edit/Bash/Grep 五类原子操作，Bash 是"图灵完备的逃逸舱" |
| **Context** | 模型每一轮"看到"什么 | 分层压缩：达到 92% 窗口触发压缩，早期消息摘要化，但 CLAUDE.md / 系统提示词 / 工具定义**每次压缩后重新注入** |
| **Memory** | 跨会话的知识怎么持久化 | 两层：CLAUDE.md（显式）+ `~/.claude/memory/`（隐式，自动记录偏好） |
| **Hooks** | 事件发生时怎么自动触发副作用 | 工具执行前后挂自定义逻辑（如保存文件前自动格式化） |
| **Permissions** | 哪些行动可以直接做、哪些要人卡 | 三档：自由使用 / 需要审批 / 完全禁止，支持命令级白名单（如"允许 Bash 但只允许 npm test"） |

### 数据流：Agentic Loop

所有 Harness 的心脏形状都一样，这是横切 8 个框架后得到的最强共性：

```
User Input
  -> Build Context      （Context 组件：装配 messages + memory + skills + profile）
  -> Call Model          （Model：唯一的"思考"环节，其余都是 Harness 的工程活）
  -> Parse Response
  -> Dispatch Tools / Handoff / Ask User   （Permissions 在这里插入审批门控）
  -> Collect Observations
  -> Update State        （Memory 在这里做压缩/摘要/遗忘）
  -> Continue or Stop
```

关键认知：**模型只负责"思考"这一环，其余六个环节全部是 Harness 的工程问题**——即便是"调用模型"本身，站在七脉分类里也该归为 **Action**（对外行动），而不是 Perception 或 Reasoning，因为它消耗预算、改变状态、产生不可逆后果（账单）。

### 设计哲学（三条压得住的结论）

1. **少而精优于大而全**——Claude Code 只给 5 个原子工具，复杂行为靠组合涌现，而不是造一堆专用工具。工具面越窄，模型的决策空间越可预测。
2. **重新注入优于长期记忆幻觉**——CLAUDE.md 显得"持久"，根本原因不是模型记住了，是 Harness 每轮都把它塞回上下文。这打破了一个常见误解：不要指望模型自己维持长期一致性，要靠 Harness 的机械重复。
3. **Harness 决定了同一个模型的能力上限能不能被兑现**——模型换来换去，Harness 配置（规则、Skills、工具集）才是真正决定工程产出质量的变量。

---

## 二、逆向五步法复盘（通用方法论，不限于 Harness）

这是本次学习里最值得反复使用的部分——**不是 Harness 的知识，是"如何拆解任何一个 Agent 框架"的方法论**。

### 起点：三个必须绕开的坑

| 坑 | 表现 | 为什么错 |
|---|---|---|
| README 幻觉 | 读完 README 只知道"能做什么"，不知道"怎么做的" | README 是产品文档，天然不回答架构问题（主循环在哪、权限卡在哪一步） |
| 关键词漫游 | grep "tool"、grep "memory"，几十个文件各看两行 | 没有预期框架，看到的都是碎片，拼不成结构 |
| 从 main() 一路钻到底 | 花半小时看 CLI 参数解析，还没碰到主循环的边 | 大型项目里 60-70% 代码是配置/日志/类型定义/UI glue，main() 是入口不是脊柱 |

**核心破局技巧：不要从 `main()` 开始，从 `tool_call` 开始。** 因为 `main()` 是程序的入口，但程序的**逻辑脊柱**是主循环——从 tool_call 往回看，第一行代码就在讲 Agent 的本质（模型提出工具调用 → 系统怎么解析 → 匹配注册表 → 绑定参数 → 执行 → 结果回下一轮）。这条经验的适用范围不止读源码——**理解任何复杂 Agent 系统，都不要从"它怎么启动"入手，要从"它每次循环里怎么决策"入手。**

### 五步法：Detect → Classify → Filter → Map → Verify

#### Step 1｜Detect：找主循环

用三组关键词交叉定位，而不是单独 grep 一个词：

```bash
rg "tool_call|function_call|ToolCall|Action|Observation" .   # 行动侧
rg "while|loop|step|run|turn|conversation" .                  # 循环侧
rg "messages|context|history|state" .                          # 状态侧
```

三组关键词的交集所在文件，大概率就是主循环。产出物应该是一张 **不超过 10 个框** 的管线图（多了说明还没抓住主干）。

#### Step 2｜Classify：先分类，别急着深挖

找到主循环后，把周围组件按"七脉"（Perception / Memory / Reasoning / Action / Governance / Collaboration / Observability，具体脉络以自己项目的分类框架为准）挂到主循环的节点上。这一步的价值在于：**归类本身会暴露认知偏差**——比如把"LLM 调用封装层"直觉归为"模型层"，但按功能归类它其实是 Action（对外行动，花钱、改状态）。分类不对，后面全错。

#### Step 3｜Filter：敢于不读

标准只有一句话：**这个文件会不会改变 Agent 的决策、上下文、状态、行动或权限？不会，就标 boilerplate，先放过。**

实操中约 60-70% 的代码量会被过滤掉（CLI 参数解析、UI/TUI、错误文案、测试 fixture、schema 类型定义）。这一步做起来会"痛苦"——总想看懂每个文件——但这正是效率的来源：问题从"这个文件是干什么的"变成"这个系统在哪里控制上下文、控制行动、控制状态、控制权限"，后者才是架构师该问的问题。

#### Step 4｜Map：落到双轴矩阵，而不是贴标签

**Map 不是分类完就结束。** 每个组件要能回答三个问题：
1. 解决哪个认知功能？
2. 采用哪种拓扑（链式 Chain / 路由 Route / 循环 Loop / 分层 Hierarchy...）？
3. 失败模式是什么？

示例句式（可套用在任何框架上）：

> `<组件名>` → `<功能脉> × <拓扑>`
> 描述：它具体怎么工作（一两句话说清楚"为什么是这个拓扑，不是别的"）
> 失败模式：这个设计会在什么情况下坏掉

三个实战案例（来自对 Hermes 自身的 Map）：
- **Context Assembler → Perception × Route**：按会话状态/skills/偏好从多来源路由拉取上下文片段，不是顺序链式拼接；失败模式是路由条件写错、重要上下文被跳过。
- **Memory Manager → Memory × Loop**：每轮结束压缩、更新摘要、检查遗忘，是持续迭代而非一次写入；失败模式是压缩过狠丢信息，或遗忘策略太保守导致上下文膨胀。
- **Tool Loop Guardrails → Governance × Loop**：每轮工具调用后检查状态，连续失败触发告警/硬停；失败模式是阈值过宽（问题已扩散）或过窄（正常重试被误杀）。

做完这一步，系统从"一堆配置项"变成"一张有若干模式坐标的热力图"——能看到哪些格子在忙碌、哪些格子是空白（比如个人助手型系统的 Collaboration×Hierarchy 格通常是空的）。

#### Step 5｜Verify：回到源码，不然就是讲故事

证据分三层，强度递减：**源码（最强）> 测试（也很强）> 官方文档（辅助）**。

Verify 的价值不是"确认自己是对的"，而是**纠正凭直觉建立的错误认知**。例如：曾以为审批门控是在工具调度阶段检查，读源码后发现它其实是在"模型提出工具调用之后、真正执行之前"的独立检查点——这个位置差异直接决定了它是 Governance×Route（人工确认路径）而不是简单的前置校验。

### 五步法的可迁移用法（横向扩展）

拆完一个框架之后，横向切多个框架时，可以把五步法压缩成四个统一问题去逐个框架提问，效率更高：

1. 它主要解决什么问题？
2. 它抓住了什么工程关键点（即"深潜点"在哪个地基）？
3. 放在当下，它最值得观察的新变化是什么？
4. 用五步法读它，第一步应该从哪里下手（即 tool_call 在哪个文件）？

这套"四问模板"本质上是五步法的压缩版，专门用于**横向对比多个框架**而不是纵向吃透一个框架。

---

## 三、八框架横切对照（Harness vs 其他 7 个框架）

八个框架用同一把刀切开后，抽出 **5 个共同地基** 和 **3 种工程性格**。这是本轮学习里抽象层级最高、复用价值最大的结论。

### 五个共同地基（给任何 Agent Harness 照 X 光）

| 地基 | 核心问题 | 常见缺口 |
|---|---|---|
| 主循环 | 用户输入→模型→工具→状态更新的心跳是否闭环 | 循环里某个环节被隐式跳过（如状态更新在异常路径下不触发） |
| 上下文管理 | 上下文怎么分诊、压缩、重注入 | 没有"分诊"——所有信息平等进入、平等压缩，导致重要信息和噪声一起被砍 |
| 工具注册与执行 | 工具描述质量、参数绑定是否可靠 | 工具描述靠人工维护，质量随维护勤快程度波动 |
| 状态账本 | 能不能回答"刚才这一轮到底改变了什么"，能不能 replay | 有 memory/state 但不支持 replay，长任务出错后无法"还原到第 N 步重跑" |
| 治理边界 | 审批门控、护栏、爆炸半径控制是否存在 | 有门禁，没有监控录像——审计追踪缺失 |

**这五个地基最大的价值：不是评价某个框架好不好，而是照出"自己的系统里有什么自己都没意识到缺的东西"。** 例如学习者本人在核对自己的 Hermes 系统时，五个地基里有四个半具备，唯独"状态账本不支持 replay"这一项——在读到这条之前完全没意识到自己缺这个能力。

### 八个框架的关键切片（挑对工程认知冲击最大的几个）

| 框架 | 核心抽象 / 深潜点 | 可复用的工程判断 |
|---|---|---|
| **Claude Code** | Harness 比模型更重要；四条线——工具注册执行、高风险工具权限流程、子 Agent 独立上下文、工具结果回流下一轮 | 模型换了不代表行为可控性变了；真正决定"能不能长期嵌进真实工作流"的是权限流程和上下文回流机制，不是模型智商 |
| **Aider** | Git 是最小可靠账本——不造新的记忆/审计系统，直接复用 Git 做行动记录（diff 看改了什么、revert 能回退、blame 能追责） | 系统设计最难的决策是"不加什么"；如果已有基础设施（如 Git）能满足账本需求，不要重复造轮子 |
| **OpenCode** | 把 LSP 接入感知层，让 Agent 看到编译器级事实（definition/reference/diagnostics/type），而非字符串匹配猜测 | 感知层的质量上限决定了"幻觉引用"发生率；grep 感知和 LSP 感知不是量的差距，是质的差距 |
| **OpenHands** | 事件流（Event System）——行动/观察/状态全部转成 append-only 事件日志，同时承担 replay、debug、审计、恢复、前端同步 | 日志分两种："给人看的日志"（只记录发生了什么）vs "可回放的事件链"（能重建任意历史状态）；只有后者能支撑长任务失败后的恢复 |

### 三种工程性格

| 性格 | 代表框架 | 核心矛盾 | 最重的脉络 |
|---|---|---|---|
| 开发者工具型 | Claude Code / Codex CLI / Aider / OpenCode | 如何在开发者已有工作流里行动，不另造世界 | Perception、Action、Governance |
| 个人助手型 | OpenClaw / Hermes | 如何让 Agent 成为长期伴随的控制面，摆脱单次聊天窗口 | Memory、Action、Governance |
| 重执行/多 Agent 型 | DeerFlow / OpenHands | 如何把复杂任务拆开执行，又不让它失控 | Collaboration、Action、Governance、Observability |

**三类都重视 Governance，但治理重点完全不同，不能互相照抄：**

- 开发者工具型 → 治理重点是"权限和工具范围"（防止改了不该改的文件）
- 个人助手型 → 治理重点是"身份和通道"（哪个信道说话、什么权限、记忆范围怎么划）
- 重执行型 → 治理重点是"沙箱和事件"（执行怎么隔离、怎么回放）

### 选型速查表

| 需求 | 应该重点参考 |
|---|---|
| Agent 要在代码仓库里干活 | Claude Code / Aider / OpenCode |
| Agent 要主动执行多步操作 | OpenHands |
| Agent 要跨长时间使用越来越懂你 | Hermes / OpenClaw |
| 多个 Agent 要并行协作 | DeerFlow |
| Agent 需要协议层跨 surface 运行 | Codex CLI |

比选具体框架更重要的是**先确定自己的工程性格**，再决定往哪个地基堆资源——个人助手型系统不需要把 Perception 做到 Aider 的 repo map 那么深，也不需要 OpenHands 级别的多层 sandbox；该重投的是 Memory（长期记忆沉淀）和"身份/信道级"的 Governance。

---

## 四、Harness → Hermes 对照表

### 五组件逐项对照

| 组件 | Claude Code 的做法 | Hermes 的做法 | 差距 / 借鉴方向 |
|---|---|---|---|
| **Tools** | 5 类原子操作（Read/Write/Edit/Bash/Grep），少而精，复杂行为组合涌现 | 对等实现了 `read_file`/`write_file`+`patch`/`search_files`/`terminal` 四类，另加桌面控制（`cua_driver`：click/type_text/scroll/get_window_state）、浏览器控制（`browser_navigate`等）、MCP 工具自动发现注入、技能系统（`skill_view`/`skill_manage`） | Hermes 工具面更广（终端+桌面+浏览器三个交互面），Claude Code 更窄但更聚焦；**未借鉴**：Claude Code 的"少而精"哲学没有被严格遵循，Hermes 工具集更接近"够用就加" |
| **Context** | 92% 窗口触发压缩，早期消息摘要化，CLAUDE.md/系统提示词/工具定义每次压缩后**重新注入** | 两段式持久上下文：MEMORY.md（95% capacity，技术配置/工作流规则/Pitfalls）+ USER.md（97% capacity，行为偏好/语言偏好/命令速记），每次对话启动时作为 system prompt 注入 | **已借鉴**：静态文件重注入的机制一致；**未实现**：Hermes 没有运行时自动压缩——MEMORY.md 是静态快照，长任务（如连续写 10 篇博客）对话历史会无限膨胀，没有自动摘要意味着模型迟早"遗忘"早期上下文——这是 Hermes 当前最明确的短板 |
| **Memory** | 两层：CLAUDE.md（显式）+ `~/.claude/memory/`（隐式，自动从对话提取偏好写入） | MEMORY.md = CLAUDE.md 等价物，带显式字符数追踪逼迫定期整理；Skills = 程序化记忆（比纯文本规则更进一步，带触发条件+步骤指令+Pitfalls，可自动加载执行） | **超越**：Skills 是"程序式知识"，比 CLAUDE.md 的"声明式知识"更进一步；**缺口**：没有隐式记忆——必须手动要求更新 MEMORY，不会自动从对话中提炼沉淀 |
| **Hooks** | 工具执行前后挂事件驱动的自定义逻辑（如保存文件前自动格式化） | 没有显式 Hooks 系统，只有 Persona 里写的"关键词匹配式"自动加载规则（如"提到 codex CLI 就加载 codex skill"），是 Hook 的雏形，但基于关键词而非事件 | **待补**：最自然的补法是"工具调用前后触发 skill"，例如 `terminal` 返回非零 exit code 时自动加载 `systematic-debugging` skill——这个设计空间目前完全空着 |
| **Permissions** | 三档（自由使用/需审批/完全禁止），支持命令级白名单（"允许 Bash 但只允许 npm test"） | 工具级别访问控制：`cua_driver` 桌面操控需显式授权，MCP 工具可配置开关 | **缺口**：没有 Claude Code 那种细粒度命令白名单，粒度停留在"整个工具开/关"，而非"工具内部具体命令级" |

### Agentic Loop 对照

两者的主循环形状完全一致，这不是巧合，是所有 Harness 的共性：

```
Claude Code: 用户指令 → 模型推理 → 工具调用 → 结果回注 → 继续推理 → ...
Hermes:      用户输入 → 模型think → tool_call → 工具执行 → 结果注入 → 下一轮think → ...
```

### 净差距总结

**Hermes 领先的两点：**
1. 桌面操控（CUA Driver）——把任意 GUI 应用变成"可编程"，覆盖终端之外的交互面，Claude Code 完全没有。
2. 技能系统（Skills）——主动的程序化记忆，带触发条件/步骤/Pitfalls/版本管理，是"从经验中学习"的基础设施。

**Hermes 落后的两点（明确的改进方向）：**
1. **运行时上下文压缩缺失** —— 最优先要补的课，长任务质量会随 token 耗尽而下降。
2. **隐式记忆缺失** —— 自动化程度不够，依赖人工手动维护 MEMORY.md。

---

## 五、可复用工程模式清单（通用，不限于 Harness）

以下模式提炼自三篇文章，标注了适用场景，可在设计任何 Agent 系统时直接检查清单式使用。

1. **静态知识重注入 优于 依赖模型自身记忆**
   把关键规则/上下文放进每轮都会重新拼装进 prompt 的文件（CLAUDE.md / MEMORY.md），而不是指望模型"记住"。适用：任何需要长期一致行为的 Agent。

2. **工具即行动，模型调用本身也是一种（昂贵的）工具调用**
   分类系统时把"调用 LLM"本身归入 Action 脉络而非 Reasoning，因为它消耗预算、产生不可逆后果。适用：做架构分类/成本核算时避免把模型调用当"免费思考"。

3. **少而精的原子工具 + 组合涌现 优于 一堆专用工具**
   工具面越窄，模型决策空间越可预测，行为越可控。适用：设计工具集时先问"能不能用已有原子操作组合出来"，再决定要不要加新工具。

4. **审批门控要放在"模型已提议、尚未执行"的独立检查点，而不是嵌进工具调度逻辑里**
   这样高风险操作可以显式路由到人工确认路径（Governance × Route），而不是和普通调度逻辑耦合在一起，出问题不好排查。

5. **状态账本要支持 replay，不能只是"给人看的日志"**
   判断标准：能不能回答"刚才那一轮到底改变了什么"，能不能"还原到第 N 步重新走一遍"。如果已有基础设施能满足（如 Git 的 diff/revert/blame），优先复用，不要重复造轮子。

6. **感知层的质量决定幻觉率——字符串匹配 vs 结构化事实（如 LSP 的 definition/reference/diagnostics）**
   适用：任何需要"知道某个实体准确定义/引用"的场景，优先接入结构化数据源而非纯文本检索。

7. **上下文要分诊，不能所有信息平等压缩**
   区分"必须完整保留的近期信息"和"可摘要化的历史信息"，并明确哪些内容（系统提示词、工具定义、核心配置）永远不参与压缩、必须每轮重新注入。

8. **先确定"工程性格"，再决定资源投入方向，不要看到别人有什么就照抄**
   开发者工具型 / 个人助手型 / 重执行型三种性格的核心矛盾不同，Governance 的治理重点也不同（权限范围 vs 身份通道 vs 沙箱事件）。先自问"我的系统面对的是代码仓库、人的生活流、还是复杂多步任务"，再决定 Perception/Memory/Collaboration 该投多少。

9. **拆解任何新框架时，用"四问模板"代替读全部代码**
   它主要解决什么问题？抓住了什么工程关键点？最值得观察的新变化是什么？用五步法读它该从哪里下手？—— 四个问题就能定位一个框架的核心贡献，不需要通读源码。

10. **读源码 / 拆框架，从 `tool_call` 起步，不从 `main()` 起步**
    任何 Agent 系统的逻辑脊柱是主循环而非启动流程。适用：不仅是读源码，理解任何复杂 Agent 系统都应遵循"先看每轮决策，再看启动方式"的顺序。

11. **Filter 阶段要敢于不读——用"是否改变决策/上下文/状态/行动/权限"做唯一过滤标准**
    预期能过滤掉 60-70% 的代码量（CLI解析、UI、错误文案、测试fixture、类型定义）。适用：面对任何大型代码库的架构级理解任务。

12. **Map 阶段不能只贴标签，必须回答"拓扑+失败模式"**
    "这是 Memory" 不是答案，"Memory × Loop，失败模式是压缩过狠丢信息" 才是。适用：任何架构分析文档，避免流于表面分类。

---

## 六、参考材料索引

- 极客时间专栏《Claude Code 工程化实战》（黄佳）
  - 逆向五步法（上）—— 拆解单一框架的方法论来源
  - 逆向五步法（下）—— 8 框架横切对照 · 5 地基 · 3 工程性格
  - 热点加餐《Harness 架构深度解析》—— <https://time.geekbang.org/column/intro/1398>
- 本地学习笔记（本文档的原始素材）：
  - `/Users/huangcheng/blogs/geekbang-study/blogs/agent-design-patterns/blog4_逆向五步法拆源码_2025-07-19.md`
  - `/Users/huangcheng/blogs/geekbang-study/blogs/agent-design-patterns/blog5_8框架横切3种工程性格_2025-07-19.md`
  - `/Users/huangcheng/blogs/geekbang-study/blogs/agent-design-patterns/blog_harness_Harness架构深度解析_Hermes对照_2026-07-27.md`
- 涉及到的 8 个框架（横切对照对象）：Claude Code、Codex CLI、Aider、OpenCode、OpenClaw、Hermes（本人系统）、DeerFlow、OpenHands
- Anthropic 官方定义（Harness）：
  > "Claude Code serves as the agentic harness around Claude: it provides the tools, context management, and execution environment that turn a language model into a capable coding agent."
