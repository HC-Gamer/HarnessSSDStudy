[pytest]
# 12-2 的官方文件名是 eval_test.py，不符合 pytest 默认的 test_*.py 发现规则，
# 这里显式加进来（另有 tests/test_eval.py 做别名双保险）。
python_files = test_*.py eval_test.py
testpaths = tests
addopts = -q -m "not slow"
markers =
    slow: 需要真实调用 LLM 的用例（消耗 token），用 -m slow 单独跑
filterwarnings =
    ignore::DeprecationWarning
