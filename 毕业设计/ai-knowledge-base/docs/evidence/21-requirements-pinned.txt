### 检查 6：版本固定（requirements.txt 全部钉死）
$ cat requirements.txt
# AI 知识库 V3 依赖（Python 3.12）
# 安装：pip install -r requirements.txt
# 版本为 2026-08-12 本机实测通过的组合，全部钉死，避免 API 漂移。

# --- 工作流编排 ---
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1

# --- HTTP / 数据源 ---
httpx==0.28.1
PyYAML==6.0.3
feedparser==6.0.14

# --- 配置与测试 ---
python-dotenv==1.2.2
pytest==9.1.1

$ grep -E '^[a-zA-Z]' requirements.txt | grep -vc '=='   # 未固定版本的条目数（期望 0）
0
0

$ /Users/huangcheng/ai-knowledge-base/.venv/bin/python -V
Python 3.12.10
