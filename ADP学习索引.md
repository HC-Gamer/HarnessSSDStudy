# 《Agent 设计模式之美》学习索引

> 极客时间专栏，作者：黄佳。本索引整理 blog1–blog10 的学习笔记，对照 Hermes Agent 工程实践，供日后快速查阅。
> 最后更新：2026-07-27　|　已读进度：10/10（开篇词 → 双轴框架 → 逆向五步法 → 8框架横切 → 直播回放 → Loop Engineering → 感知模块导论 → 上下文分诊 → 语义压缩）

---

## 一、专栏总览

- **主题**：Agent 设计模式——把"第三代模式语言"系统化。前两代模式语言分别应对*确定性协作*（GoF 23 模式）和*网络不可靠*（分布式系统模式），第三代应对**智能不可靠**——模型的感知、推理、行动本身是概率事件。
- **核心方法论**：**双轴框架**（认知功能 × 执行拓扑的正交矩阵）+ **逆向五步法**（Detect/Classify/Filter/Map/Verify，用于读懂任何 Agent 源码或框架）。
- **核心口号**："Model spends, harness budgets"——模型负责花注意力/token/信任，Harness 负责管理这些有限资源的预算。
- **作者**：黄佳。专栏含正课、逆向工程篇、企业案例直播、以及"感知—记忆—推理—行动—反思—协作—治理"七脉的逐模块精讲（本索引覆盖到"感知"分支的前三讲：导论、上下文分诊、语义压缩）。
- **我的对照项目**：Hermes Agent（个人助手型 CLI Agent，日常使用半年+）、OpenCode（编程助理，作对比参照）。

---

## 二、知识体系图

```
开篇词（范式转移）
   │  三代模式语言 / Model spends, harness budgets
   ▼
双轴框架（上+下）
   │  纵轴：七脉认知功能（感知/记忆/推理/行动/反思/协作/治理）
   │  横轴：六式执行拓扑（链式/路由/并行/编排/循环/层级）
   │  → 42 格矩阵，28 个模式，14 个刻意留空
   │  → Pattern Selection Card (ASSESS→ROUTE→SELECT)
   │  → 双轴评审法（5问）+ Compound Error（复合错误率）
   ▼
逆向五步法（上：方法论 + 下：8框架横切）
   │  Detect（找主循环）→ Classify（归七脉）→ Filter（滤70%噪声）
   │  → Map（落矩阵坐标）→ Verify（回源码验证）
   │  → 横切 8 个真实框架，抽出 5 个共同地基 + 3 种工程性格
   ▼
【企业案例直播】范式方法论的生产级验证
   │  通用开放 Agent vs 企业执行型 Agent / 控制面-叙事面分离
   │  ReAct Scratchpad / L1-L2-L3 分层记忆 / Token·可观测性·治理
   ▼
Loop Engineering（热点加餐）
   │  三个平面：叙事（发散）/ 控制（收敛）/ 调度（协调）
   │  执行型 Agent vs 内容生成型 Agent / 复利循环（Skills 沉淀）
   ▼
感知模块（七脉第一支线：导论 → 分诊 → 压缩）
   ├─ 感知模块导论：最小高信号 token 集合 / PRA 循环 / Lost in the Middle
   ├─ 上下文分诊：急诊室分诊隐喻 / P0-P3 分级 / 分诊可观测性
   └─ 语义压缩：三层 Compactor（L1清理/L2 Anchor/L3极限）/ excluded_approaches / ObservationMasking
```

**结构定位**：
- **总纲层**（决定怎么看待整个领域）：blog1（范式认知）、blog2+blog3（双轴框架——理论骨架）、blog4+blog5（逆向五步法——方法论工具）。
- **验证/串联层**：blog6（直播回放，用企业真实案例校验前四讲的框架是否成立）、blog7（Loop Engineering，横切"推理×循环"这一格，讲清楚循环怎么工程化）。
- **具体模块层**（七脉逐一精讲，目前覆盖"感知"分支）：blog8（感知导论）、blog9（上下文分诊，感知×路由的具体模式）、blog10（语义压缩，记忆/感知交界的具体模式）。

---

## 三、逐篇摘要

### blog1｜开篇词：范式转移
**核心概念**：软件模式经历三代——GoF（确定性协作）→ 分布式系统模式（网络不可靠）→ Agent 设计模式（智能不可靠）。核心命题 **"Model spends, harness budgets"**：模型自主决定怎么花注意力/推理路径/信任额度，Harness 决定预算上限与账本结构。
**Hermes 对照**：`max_turns`、`api_max_retries`、`tool_loop_guardrails`、`compression` 等配置本质是四本"预算账本"（注意力/连续性/生成方向/信任），但过去从未被当作同一类事物统一管理；`api_max_retries` 解决的是"服务不可靠"，治不了"模型推理偏了"这种概率性偏差。
**要点**：Agent 系统同时面对网络不可靠和智能不可靠两种敌人，第三代模式是**叠加**而非替代第二代；系统价值不在于"能做什么"，而在于跑完一次后"有没有比之前更聪明"（经验沉淀），这正是 Hermes 当时的空白。

### blog2｜双轴框架（上）：认知功能 × 执行拓扑
**核心概念**：纵轴七脉——内圈（感知/记忆/推理/行动，让 Agent 能用）、中圈（反思，让 Agent 可靠）、外圈（协作/治理，让 Agent 能上线）；横轴六式——链式/路由/并行/编排/循环/层级委派。两轴交叉给每个模式唯一坐标。
**Hermes 对照**：感知（compression/tool_output截断/file_read上限，但缺分诊逻辑）、记忆（memory_char_limit + Skills=程序性记忆）、推理（max_turns 循环 + api_max_retries 属于治理越界）、治理（approvals/command_allowlist/tool_loop_guardrails，但缺审计追踪）、协作（orchestrator_enabled，但内圈未跑顺前不该急着开）。
**要点**：三圈建设有先后顺序——内圈没跑顺就搞多智能体是常见的失败模式；作者提出框架本身缺"跨会话时间维度"这一观察，留待后续验证。

### blog3｜双轴框架（下）：正交矩阵实战
**核心概念**：矩阵必须**正交**才是"坐标系"而非"分类表"——同一认知功能换拓扑，成本/延迟/错误恢复模型完全不同（如推理×链式 vs 推理×循环的对比代码）；42 格中留 14 个空格是刻意的设计判断，不是没想全。给出三步选型法 **ASSESS(七脉打分)→ROUTE(判主拓扑)→SELECT(查矩阵)**，以及双轴评审法五问。
**Hermes 对照**：作者用 ASSESS 给自己系统七脉打分（感知Heavy缺分诊/记忆Light/推理Heavy/行动Light/反思Light/协作None/治理Medium缺审计），并推导出第一版应补的 4 个模式：上下文分诊、迭代假设、审批门控、生成-批评。
**要点**：**Compound Error**——单步 95% 正确率、12 步任务整体成功率只有约 54%；应对路径为减少步数/提高单步质量/加 verification/fail fast，这四条正是后续 blog8-10（感知/分诊/压缩）要解决的问题源头。

### blog4｜逆向五步法（上）：拆自己的源码
**核心概念**：读 Agent 源码三大坑——README 幻觉、关键词漫游、从 main() 一路钻到底。解法：**不要从 main() 开始，从 tool_call 开始**。五步法 = Detect（grep 三组关键词找主循环）→ Classify（归七脉）→ Filter（敢于不读，滤掉 60-70% boilerplate）→ Map（落矩阵坐标+失败模式）→ Verify（回源码/测试验证猜测）。
**Hermes 对照**：作者亲手用五步法解剖 Hermes，发现认知与实际代码有 30% 出入；Gateway 被重新归类为 Action（而非"模型调用层"）；验证环节纠正了"审批门控在工具调度阶段检查"的错误认知，实际是 Gateway 调用之后的独立检查点。
**要点**：主循环是系统的"逻辑脊柱"，不是入口函数；这个方法论不仅用于读代码，也适用于理解任何复杂 Agent 系统的运行时结构。

### blog5｜逆向五步法（下）：8 框架横切，3 种工程性格
**核心概念**：用五步法横切 Claude Code、Codex CLI、Aider、OpenCode、OpenClaw、Hermes、DeerFlow、OpenHands 8 个框架，抽出 **5 个共同地基**（主循环/上下文管理/工具注册执行/状态账本/治理边界）和 **3 种工程性格**（开发者工具型/个人助手型/重执行多Agent型）。
**Hermes 对照**：Hermes 落在"个人助手型"，与 OpenClaw 同类，最重 Memory/Action/Governance；对照 5 地基发现"状态账本"最弱——没有 Git 式的单一可信源，也没有 OpenHands 式的可回放事件流；Aider 的"Git 是最小可靠账本"和 OpenCode 的"LSP 作为感知一等公民"被认为是最值得借鉴的设计。
**要点**：不同工程性格的治理重点完全不同（开发者工具型防越权改文件，个人助手型管身份和通道，重执行型管沙箱和事件），不能照抄别人的治理设计；本篇是"范式觉醒"模块的收尾。

### blog6｜直播回放：企业案例拆解
**核心概念**：ADPS 团队梁博分享企业级 Agent 化实践，验证前五讲理论在真实生产场景的落地。关键分野：**通用开放 Agent**（内容生成为主，错误代价低）vs **企业执行型 Agent**（异构 API 严格参数绑定，错误代价大）；提出 **控制面/叙事面分离**、**ReAct Scratchpad**、**L1/L2/L3 分层记忆**（对应 CoALA 的 Working/Episodic/Semantic Memory）。
**Hermes 对照**：作者反思 Hermes 早期把所有工具平铺、让 LLM 自由决策导致的两次事故（rm -rf 参数漂移、误删 skill）；类比自己折腾 UE5 MCP 插件时遇到的同样"参数漂移"问题，得出 MCP 只解决"发现工具"，不解决"执行确定性"；Hermes 现有的"意图识别"完全靠自由文本输出，缺少显式的控制面 intent block。
**要点**：Token 预算、可观测性、治理是生产级 Agent 的三个硬指标，Hermes 在可观测性和声明式治理配置上都还很初级；黄佳"没有治理就没有好 Agent"是全场最重的一句话。

### blog7｜Loop Engineering：如何把 Agent 的循环工程化
**核心概念**：把 Agent 循环拆成 **三个平面**——叙事平面（非结构化文本，发散）、控制平面（离散信号，收敛）、调度平面（状态机，协调）；区分 **执行型 Agent**（错了是真金白银损失）与 **内容生成型 Agent**（错了是主观分数差）。
**Hermes 对照**：作者把自己的 `思考→工具调用→观察→再思考` 循环映射进三平面，指出最常崩的地方是控制平面的**参数漂移**（如 session_id 被模型"创造"而非取自实际返回值）；Skills 系统被认定为 Loop Engineering 里"变自生变"的复利效应实践——每个 skill 都是一次失败经验的归档。
**要点**：确定性的值必须由程序机械掌控，不能交给模型（"让模型管控制平面就像让诗人管账本"）；"计划活在代码里，创意活在上下文里"；不能让循环自己给自己打分，验证必须来自循环外部。

### blog8｜感知模块导论：从"喂什么"到"怎么喂"
**核心概念**：感知工程师的工作是找出**"使下一次推理质量最大化的最小高信号 token 集合"**（Anthropic 定义）。感知不是检索（塞满），是筛选（塞对）。PRA 循环（Perception→Reasoning→Action）里，感知决定推理的上限——模型看不到的事实对它不存在，噪声太多会稀释真正重要的证据（**Lost in the Middle** 效应）。
**Hermes 对照**：作者用自己重构 415 个 skills 索引的真实案例做类比——全量展开 SKILL.md 是"塞满"（~800K tokens），索引+按需 `skill_view` 加载才是"塞对"（~15K tokens）；MEMORY→USER PROFILE→available_skills 的排序本身就是感知工程，用于对抗 Lost in the Middle。
**要点**：三个可直接用作设计清单的问题——①什么信息有资格进入上下文？②什么该被压缩/排序/丢弃？③什么必须在推理前就结构化好？感知与记忆的边界：感知管"这轮需要什么"（跨 turn 过期），记忆管"跨轮要保留什么"（持久化）。

### blog9｜上下文分诊：给 Agent 装一个急诊室分诊台
**核心概念**：**Context Triage**，用急诊室分诊护士做比喻——护士不需要是最聪明的人，只需在信息入口处快速判断"多急/什么形式/离模型多近"。信息分四级：**P0**（system prompt/错误堆栈，必须看到）、**P1**（当前文件/最近工具结果，优先使用）、**P2**（历史摘要/目录索引，压缩进入，需保留结论+证据+索引）、**P3**（全量仓库/日志，留在工具后按需展开）。
**Hermes 对照**：作者给出真实事故——3000 行日志原样塞进上下文，把 P0 级别的错误堆栈挤到中间，导致模型分析了无关的 WARN；随后设计了一个 `Priority` 分诊器代码，核心是"P3 永不预加载 + 错误信息强制保护"（`is_error` 特判：即使超预算也要进）。对照 Claude Code 的 CLAUDE.md（人工锁 P0，`@import` 不省 token）、Aider 的 RepoMap（算法发现结构重要性）、DeerFlow 的 schema 强制字段（tenant_id/user_id 不可漏）三条路线。
**要点**：分诊不是删材料，是控制信息与模型的**距离**；必须可观测——分诊压力（p95/p99 dropped_count）、关键层丢失（p0_dropped_count 非零即 bug）、P3 回取率（暴露的 handle 有没有真正被读取）；心智模型转变：上下文窗口是急诊室，不是数据库。

### blog10｜语义压缩：别让 Agent 在同一个坑里摔两次
**核心概念**：**语义压缩（Semantic Compaction）不是 summarize，是工作记忆维护**。三层 Compactor——**L1**（清理冗长 tool 输出，低风险）、**L2**（合并进 Anchor：意图/已做改动/已做决策/**已排除方案**/下一步，中风险，触发于 context 60-70%）、**L3**（极限压缩，高风险，应视为退场/交接信号）。类比特德·姜"ChatGPT 是网上所有文本的模糊图像"——压缩多次如同反复翻拍照片，细节永久丢失。
**Hermes 对照**：作者以自己解析 Safari Cookies 文件三次失败为引子，指出压缩后的历史摘要抹掉了"struct 偏移量越改越错"这个模式；审视 Hermes 现状发现三个缺陷——① MEMORY 区无重要性分层（UE5启动参数和品牌隔离规则混在一起）；② 缺 **in-session excluded_approaches**（同一 session 内学到的"排除项"活不过下一次压缩，只能靠人工写进 SKILL 做跨 session 沉淀）；③ 可以借鉴 OpenHands 的 **ObservationMasking**（Action 保留，早期 Observation 替换为占位符+handle）来处理 terminal 大量输出。
**要点**：压缩安全感来自"关键证据的保护"（错误堆栈=Agent 的测试覆盖率，压没等于重构时删了测试）；给出 Hermes 具体改造方案——MEMORY 加 CRITICAL/IMPORTANT/ROUTINE 标记、session 内自动记录 `[EXCLUDED]` 段、terminal 输出按 error/成功分流处理。

---

## 四、关键洞察汇总（跨篇主题）

### 1. 认知框架：从"感觉"到"坐标"
- **Model spends, harness budgets**（blog1）是贯穿全专栏的元原则：所有具体模式最终都在回答"这是哪种预算、谁来管"。
- 双轴矩阵（blog2/3）的价值不是分类，而是让"这个系统哪里弱"变得**可回溯、可评审**——从"感觉不对"变成"推理×循环没有停止条件"这种具体表述。
- **Compound Error**（blog3）把"选拓扑"和"错误复合率"绑定，是后续所有工程改进（减步数/提质量/加校验/fail fast）的数学依据。

### 2. 工程拆解方法：逆向五步法的普适性
- "不要从 main() 开始，从 tool_call 开始"（blog4）——理解任何 Agent 系统都应先抓主循环（逻辑脊柱），再归类、过滤、映射、验证。
- 8 框架横切（blog5）证明：**同一套五步法既能拆自己的代码，也能横向比较整个行业**；5 地基 + 3 性格是给任何 Agent 项目做自检的标准问卷。
- 直播案例（blog6）证明这套方法论在真实企业场景同样成立——控制面/叙事面分离、L1-L3 分层记忆，都能对号入座回双轴矩阵坐标。

### 3. Loop Engineering：循环是 Agent 的心脏
- 三平面分离（叙事发散/控制收敛/调度协调，blog7）是对 blog6"控制面-叙事面分离"的进一步细化，加入了调度平面。
- **确定性值必须由程序机械掌控**——这是贯穿 blog6/7/9/10 的一条反复出现的红线：参数漂移（blog6/7）、excluded_approaches 活不过压缩（blog10）、P0 信息必须强制保护（blog9），本质都是同一个问题的不同表现：**哪些东西不能交给模型的概率性判断**。
- Skills 系统的"复利效应"——不仅是记忆机制（blog2），也是 Loop Engineering 里"变自生变"的具体实现（blog7），还承担着跨 session 的 excluded_approaches（blog10）。三篇从不同角度指向同一个系统组件，说明 Skills 是 Hermes 当前工程实践中最成熟的一块拼图。

### 4. 感知层设计：三篇一条主线
- **导论（blog8）**定义问题：最小高信号 token 集合，感知决定推理上限。
- **分诊（blog9）**给出机制：P0-P3 优先级 + 距离控制 + 可观测指标，是感知×路由的具体落地模式。
- **压缩（blog10）**给出时间维度：分诊解决"这一轮该看什么"，压缩解决"多轮之后还能不能记得该看什么"——两者互补，压缩是分诊在时间轴上的延伸。
- 三篇共享一个诊断工具：**Lost in the Middle**（blog8）解释了为什么日志堆积会挤走错误堆栈（blog9 的真实事故），也解释了为什么反复压缩会让关键证据"翻拍到细节丢失"（blog10 的比喻）。

### 5. 治理与可观测性：反复出现的"薄弱环节"自我诊断
- blog1 就发现审批有门禁没有监控录像（缺审计追踪）。
- blog5 发现状态账本最弱，没有 Git 式可信源或事件回放能力。
- blog6 发现意图识别完全靠自由文本，没有结构化 intent block。
- blog7 承认可观测性做得不够，没有统一轨迹视图。
- blog9 给出了具体可落地的三个分诊可观测指标。
- **这是一条清晰的技术债务线**：Hermes 在感知/记忆上进展明显（已有具体分诊器代码原型），但在治理可观测性/审计/状态账本上仍是全篇反复暴露却尚未系统解决的问题。

---

## 五、建议的阅读顺序与学习路径

### 路径一：按专栏原生顺序（推荐首次通读）
blog1 → blog2 → blog3 → blog4 → blog5 → blog6 → blog7 → blog8 → blog9 → blog10
建立"范式认知→理论框架→方法论工具→生产验证→循环细化→感知支线"的完整脉络，与专栏本身的知识建构顺序一致。

### 路径二：按"总纲 vs 模块"分层复习（适合日后快速回顾）
1. **先读总纲三篇**建立坐标系：blog1（范式）→ blog2+blog3（双轴矩阵，重点看 ASSESS/ROUTE/SELECT 三步法和五问评审法）→ blog4+blog5（逆向五步法，重点看 Detect 从 tool_call 开始 + 5地基3性格自检表）。
2. **再读验证篇**看理论落地：blog6（企业案例）→ blog7（Loop Engineering，重点看三平面模型）。
3. **最后读模块篇**做具体工程参考：blog8 → blog9 → blog10（感知支线三部曲，建议连续读，因为分诊解决空间维度、压缩解决时间维度，二者互补）。

### 路径三：按 Hermes 工程优先级排序（适合指导下一步开发）
若要直接推动 Hermes Agent 改进，建议按"痛点严重程度"排序精读：
1. **blog9（上下文分诊）**——已有真实事故（日志挤走错误堆栈），且文中已给出可直接抄的 `Priority` 分诊器代码。
2. **blog10（语义压缩）**——已有真实事故（三次同坑），且给出 MEMORY 分层 + `[EXCLUDED]` 段 + ObservationMasking 三个具体改造方向。
3. **blog3（Compound Error + 选型三步法）**——重新给 Hermes 七脉打分，看当前哪些格子仍然空缺。
4. **blog6（控制面/叙事面分离）**——评估是否要把 intent 从自由文本升级为结构化 block，降低参数漂移风险。
5. **blog5（状态账本）**——评估是否引入 Git 式或事件流式的单一可信源，补齐"能不能回放"的能力缺口。

### 后续关注
专栏后续应该会继续七脉的其余分支（记忆、推理、行动、反思、协作、治理的逐讲精讲）——目前只覆盖到感知支线的三讲。建议保持追更节奏，每读完一讲即按本文档的摘要模板补充，并同步更新第四节"关键洞察汇总"里的技术债务线。
